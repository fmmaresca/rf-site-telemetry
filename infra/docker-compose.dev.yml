services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: rftelemetry
      POSTGRES_USER: rftelemetry
      POSTGRES_PASSWORD: rftelemetry
    ports:
      - "5432:5432"
    volumes:
      - rftelemetry_pg:/var/lib/postgresql/data

volumes:
  rftelemetry_pg:
